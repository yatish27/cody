Here are the code reviews you were tagged in since yesterday:
<%- @pending_reviews.each do |review_name, reviews| -%>

## <%= review_name %>

<%- reviews.each do |review| -%>
* [<%= review.pull_request.repository %>#<%= review.pull_request.number %>](<%= pull_request_url(review.pull_request) %>)
<% end %>
<% end %>

--

You are recieving this email because you have enabled code review digest emails.
